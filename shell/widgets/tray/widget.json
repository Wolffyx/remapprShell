{
    "id": "tray",
    "apiVersion": 1,
    "name": "System tray",
    "description": "StatusNotifierItem icons from running applications, with the rest behind a chevron.",
    "icon": "preferences-system-tray",
    "zones": [
        "left",
        "middle",
        "right"
    ],
    "orientation": [
        "horizontal",
        "vertical"
    ],
    "renderers": {
        "quickshell": {
            "entry": "Widget.qml"
        },
        "plasma": {
            "applet": "org.kde.plasma.systemtray"
        }
    },
    "config": {
        "iconSize": {
            "type": "int",
            "default": 0,
            "min": 0,
            "max": 48,
            "label": "Icon size",
            "description": "0 follows the panel's tray icon size."
        },
        "chevron": {
            "type": "enum",
            "values": ["after", "before"],
            "labels": ["After the icons", "Before the icons"],
            "default": "after",
            "label": "The hidden-icons button sits",
            "description": "after: at the end of the tray, past the icons, as the design has it. before: at the start, so the icons that come and go do not push it around -- which is what Plasma's tray does."
        },
        "pinned": {
            "type": "list",
            "default": [],
            "label": "On the panel",
            "description": "StatusNotifierItem ids shown on the panel, in this order. Empty shows every item; pin any and the rest move behind the chevron. Settings has a page that edits this by dragging, which is easier than typing ids."
        },
        "hidden": {
            "type": "list",
            "default": [],
            "label": "Never shown",
            "description": "Ids left out altogether, not even behind the chevron."
        }
    }
}
