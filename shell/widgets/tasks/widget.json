{
    "id": "tasks",
    "apiVersion": 1,
    "name": "Open windows",
    "description": "The windows you have open, and a click to switch to one. Needs the window list turned on: rmpr windows enable.",
    "icon": "preferences-system-windows",
    "zones": [
        "left",
        "middle",
        "right"
    ],
    "orientation": [
        "horizontal"
    ],
    "renderers": {
        "quickshell": {
            "entry": "Widget.qml"
        },
        "plasma": {
            "applet": "org.kde.plasma.icontasks"
        }
    },
    "config": {
        "groupByApp": {
            "type": "bool",
            "default": true,
            "label": "Group windows by application",
            "description": "One button per application, as KDE and Windows do, with a mark per window. Clicking moves through that application's windows."
        },
        "pinned": {
            "type": "list",
            "default": [],
            "label": "Pinned applications",
            "description": "Desktop entry ids (\"org.kde.dolphin\"), kept on the taskbar in this order whether or not they are running. Right-click a button and choose \"Pin to taskbar\" rather than typing them."
        },
        "thisScreenOnly": {
            "type": "bool",
            "default": false,
            "label": "Only this screen's windows",
            "description": "Each monitor's panel lists the windows on that monitor, as Windows does with \"show taskbar apps on the taskbar where the window is open\"."
        },
        "showTitles": {
            "type": "bool",
            "default": true,
            "label": "Show titles",
            "description": "The window's title beside its icon, as far as the widest a button gets. Off, buttons are icons alone and the title is one hover away."
        },
        "maxWidth": {
            "type": "int",
            "default": 230,
            "min": 60,
            "max": 400,
            "label": "Widest a button gets",
            "description": "Titles are elided past this."
        },
        "iconSize": {
            "type": "int",
            "default": 0,
            "min": 0,
            "max": 48,
            "label": "Icon size",
            "description": "0 follows the panel's thickness, so resizing the panel resizes the icons with it."
        }
    }
}
