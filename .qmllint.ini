[General]
AdditionalQmlImportPaths=
DisableDefaultImports=false
OverwriteSettings=false

[Warnings]
# Quickshell exports PanelWindow (and friends) as non-creatable interfaces and
# substitutes a concrete backend at runtime. Creating one is correct usage;
# qmllint cannot see the substitution.
UncreatableType=disable
