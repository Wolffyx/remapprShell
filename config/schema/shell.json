{
    "comment": "Describes every setting so the GUI can render controls for it, and so docs/config.md can be generated from it. A section carrying both `page` and `keys` renders as the page -- that wins -- while the keys still reach the generated reference, which is how a setting that needs more than a control in the window is still documented as the config key it is. Deliberately the same shape as the `config` block in a widget.json, so one renderer handles built-in settings and third-party widgets alike.",
    "sections": [
        {
            "id": "appearance",
            "label": "Appearance",
            "icon": "preferences-desktop-theme-global",
            "glyph": "palette",
            "page": "appearance",
            "description": "How the shell itself looks -- light or dark, its accent, its corners -- and the style every Qt application is drawn in. The shell's own colours change nothing outside it.",
            "keys": {
                "theme.mode": {
                    "type": "enum",
                    "values": [
                        "auto",
                        "light",
                        "dark"
                    ],
                    "default": "auto",
                    "label": "Colour scheme",
                    "description": "auto is light by day and dark by night, on KWin's Night Light schedule -- the same sunset that warms your screen, set in System Settings under Display and Monitor. Plasma has no light/dark switching of its own (its sunset switching is for wallpapers), so with Night Light turned off there is no schedule to follow and auto instead turns dark whenever the Plasma colour scheme is dark, following it the moment it changes. light and dark hold it where they say."
                },
                "theme.accent": {
                    "type": "enum",
                    "values": [
                        "plasma",
                        "blue",
                        "teal",
                        "magenta",
                        "orange"
                    ],
                    "default": "plasma",
                    "label": "Accent",
                    "description": "plasma is Plasma's own accent colour, which System Settings can also take from the wallpaper. Every other colour the shell uses is worked out from this one, in Material Design's roles. A colour written as #rrggbb is accepted too."
                },
                "theme.translucent": {
                    "type": "bool",
                    "default": true,
                    "label": "Translucent surfaces",
                    "description": "The panel and its popouts let a little of what is behind them through. Off draws them solid."
                },
                "theme.shadows": {
                    "type": "bool",
                    "default": false,
                    "label": "Drop shadows",
                    "description": "A soft shadow under the panel, its popouts, the start menu and the on-screen display. Off by default: a shadow is a band of dimmed wallpaper around a surface whose own background is blurred, and on a dark desktop the join between the two reads as a second panel behind the first."
                },
                "theme.rounding": {
                    "type": "int",
                    "min": 0,
                    "max": 36,
                    "step": 2,
                    "default": 8,
                    "label": "Corner rounding",
                    "description": "The radius of the largest surfaces -- the start menu, quick settings, a floating panel. Smaller things are rounded in proportion."
                },
                "theme.animationMs": {
                    "type": "int",
                    "default": 180,
                    "min": 0,
                    "max": 400,
                    "step": 20,
                    "label": "How long things take to appear",
                    "description": "In milliseconds: a popout rising out of the panel, the switcher, the desktop overview. 0 makes them appear at once, which is the fastest the shell can feel and the least it can explain -- a surface that simply exists gives no hint about where it came from."
                },
                "theme.desktop.enabled": {
                    "type": "bool",
                    "default": true,
                    "label": "Theme the whole desktop",
                    "description": "Applying the theme also re-themes KDE itself, so applications match the shell rather than only the panel and its popouts. Off confines the theme to what this shell draws. Each part below can be left out; anything left out keeps whatever you have chosen in System Settings, and `rmpr theme revert` puts every part back."
                },
                "theme.desktop.followMode": {
                    "type": "bool",
                    "default": false,
                    "label": "Applications follow day and night",
                    "description": "With Colour scheme set to auto, the colour scheme and icon theme KDE itself uses are rewritten when night falls, so applications turn dark with the shell instead of staying wherever they were last put. Off by default: it writes KDE's own configuration on a schedule, which is not something to do to a desktop uninvited. `rmpr theme variant` does it once, by hand, and `rmpr theme revert` puts it all back. Plasma's own widgets follow from the next plasmashell start."
                },
                "theme.desktop.gtk": {
                    "type": "bool",
                    "default": true,
                    "label": "GTK applications follow too",
                    "description": "Chrome, Electron applications and GTK applications do not read KDE's colour scheme: they ask a portal, and the GTK portal answers from its own dark/light preference. With this on, that preference is written with the rest -- which is what makes them turn light by day and dark by night instead of staying wherever they were. `rmpr theme revert` puts the preference back."
                },
                "theme.desktop.gtkThemeLight": {
                    "type": "string",
                    "default": "",
                    "label": "GTK theme by day",
                    "description": "The GTK theme to wear in light mode, by name. Empty leaves the theme name alone, which is the default. Asking GTK to prefer light is not the same as giving it a light theme: a theme whose name is the dark half of its pair -- Nordic, adw-gtk3-dark -- ignores the preference and stays dark in every mode. Name both halves here and the pair follows day and night. Not guessed from the other name: adw-gtk3's light half drops \"-dark\", Nordic's is called Nordic-Polar, and a wrong guess puts you in a theme you never chose."
                },
                "theme.desktop.gtkThemeDark": {
                    "type": "string",
                    "default": "",
                    "label": "GTK theme by night",
                    "description": "The GTK theme to wear in dark mode, by name. Empty leaves the theme name alone. See GTK theme by day."
                },
                "theme.desktop.materialYou": {
                    "type": "bool",
                    "default": false,
                    "label": "kde-material-you-colors follows too",
                    "description": "kde-material-you-colors derives a colour scheme from your wallpaper and applies it to the session -- at login, and again whenever the wallpaper changes. It has its own light/dark switch, so left alone it is the last writer at every login and the desktop wears its answer rather than this shell's. With this on, `rmpr theme variant` writes that switch and restarts the unit so the two agree. Off by default: it is somebody else's service, and rewriting a configuration this project does not own is not something to do uninvited. Ledgered, so `rmpr theme revert` puts it back."
                },
                "theme.desktop.colours": {
                    "type": "bool",
                    "default": true,
                    "label": "Colour scheme",
                    "description": "The Plasma colour scheme every Qt application is drawn with. Off leaves whatever you have chosen in System Settings."
                },
                "theme.desktop.icons": {
                    "type": "bool",
                    "default": true,
                    "label": "Icon theme",
                    "description": "The icon theme, for applications and for the shell's own icons, which come from it rather than from a set of our own."
                },
                "theme.desktop.style": {
                    "type": "bool",
                    "default": true,
                    "label": "Widget style",
                    "description": "The Qt widget style applications are drawn in -- buttons, scrollbars, checkboxes."
                },
                "theme.desktop.plasmaTheme": {
                    "type": "bool",
                    "default": true,
                    "label": "Plasma desktop theme",
                    "description": "The theme Plasma's own surfaces use: the desktop, its widgets, and anything the shell does not draw itself."
                },
                "theme.desktop.decorations": {
                    "type": "bool",
                    "default": true,
                    "label": "Window decorations",
                    "description": "The titlebars and borders KWin draws around windows."
                },
                "theme.desktop.switcher": {
                    "type": "bool",
                    "default": true,
                    "label": "Alt+Tab switcher",
                    "description": "The window switcher's layout."
                }
            }
        },
        {
            "id": "taskbar",
            "label": "Taskbar",
            "icon": "configure",
            "glyph": "view_agenda",
            "page": "taskbar",
            "description": "The panel: where it sits, how big it is, and the widgets that are parts of it -- the clock, the workspace pills and the window buttons. Every one of these is a configuration key, so a profile can set them by hand as well.",
            "keys": {
                "panel.position": {
                    "type": "enum",
                    "values": [
                        "top",
                        "bottom",
                        "left",
                        "right"
                    ],
                    "default": "bottom",
                    "label": "Position",
                    "description": "Which edge the panel is anchored to."
                },
                "panel.thickness": {
                    "type": "int",
                    "min": 28,
                    "max": 96,
                    "step": 2,
                    "default": 52,
                    "label": "Thickness",
                    "description": "Height of a horizontal panel, width of a vertical one. Its buttons grow and shrink with it."
                },
                "panel.style": {
                    "type": "enum",
                    "values": [
                        "full",
                        "floating",
                        "islands"
                    ],
                    "default": "full",
                    "label": "Style",
                    "description": "full is a strip along the whole edge. floating is a rounded bar held clear of the edge. islands draws no bar at all: each zone -- the start button and workspaces, the windows, the tray and clock -- is a rounded island of its own."
                },
                "panel.spacing": {
                    "type": "int",
                    "min": 2,
                    "max": 16,
                    "default": 5,
                    "label": "Spacing",
                    "description": "The gap between widgets, in pixels."
                },
                "panel.iconSize": {
                    "type": "int",
                    "min": 15,
                    "max": 26,
                    "default": 18,
                    "label": "Tray icon size",
                    "description": "The size of the tray's and the status icons, in pixels."
                },
                "panel.revealOnHover": {
                    "type": "bool",
                    "default": true,
                    "label": "Reveal on hover",
                    "description": "With hiding on, the panel comes back when the pointer reaches the screen edge. Off, it comes back only when something opens from it -- the launcher from a key, say."
                },
                "panel.autoHide": {
                    "type": "bool",
                    "default": false,
                    "label": "Hide until pointed at",
                    "description": "The panel shrinks to a sliver and comes back when the pointer reaches the screen edge. It reserves no space while hidden, so windows use the whole screen."
                },
                "panel.menu.systemMonitor": {
                    "type": "string",
                    "default": "auto",
                    "label": "System monitor",
                    "description": "Which application the right-click menu's System monitor row opens, as a desktop entry id. `auto` picks the first of the usual ones that is installed; `none` leaves the row off. A monitor that is not installed is not offered, and the row is hidden rather than shown and refusing."
                },
                "panel.menu.entries": {
                    "type": "list",
                    "default": [],
                    "label": "Custom menu entries",
                    "description": "Extra rows on the panel's right-click menu, in this order. Each is an object: `label` is the words on the row, `command` is a shell command line run when it is chosen, and `glyph` is an optional Material Symbols name for its icon (`terminal` when left out). The command is run detached, so a script that keeps running does not end when the menu closes."
                }
            }
        },
        {
            "id": "sidebar",
            "label": "Sidebar",
            "icon": "sidebar-show",
            "glyph": "view_sidebar",
            "description": "The panel that slides in from an edge: what is playing, the day and the weather, the machine, and the latest notifications. Opened with a shortcut ('rmpr shortcuts set sidebar'), a screen edge ('rmpr edges shell'), or the launcher's Sidebar action.",
            "keys": {
                "sidebar.position": {
                    "type": "enum",
                    "values": [
                        "right",
                        "left"
                    ],
                    "default": "right",
                    "label": "Edge",
                    "description": "Which side it slides in from. A screen edge bound to the sidebar follows this, so the edge you push into is the side it appears on.",
                    "labels": [
                        "Right",
                        "Left"
                    ]
                },
                "sidebar.trigger": {
                    "type": "enum",
                    "values": [
                        "drag",
                        "hover",
                        "none"
                    ],
                    "default": "drag",
                    "label": "Opened by",
                    "description": "'drag' is a thin strip down the sidebar's own edge that you press and pull inwards -- a pointer resting there does nothing, so it cannot open by accident. 'hover' is KWin's screen edge instead ('rmpr edges shell'), which opens on a pointer that merely reaches the edge. 'none' leaves the shortcut and the launcher action as the only ways in.",
                    "labels": [
                        "Pulled from its strip",
                        "Pointer at the edge",
                        "Only a key or the launcher"
                    ]
                },
                "sidebar.handleWidth": {
                    "type": "int",
                    "min": 2,
                    "max": 24,
                    "step": 1,
                    "default": 6,
                    "label": "Grab strip",
                    "description": "How wide the strip you pull is, in pixels. It sits at the very edge of the screen, so a click that far out goes to it rather than to the window beneath."
                },
                "sidebar.width": {
                    "type": "int",
                    "min": 280,
                    "max": 720,
                    "step": 4,
                    "default": 396,
                    "label": "Width",
                    "description": "How wide the panel is, in pixels."
                },
                "sidebar.margin": {
                    "type": "int",
                    "min": 0,
                    "max": 64,
                    "step": 2,
                    "default": 16,
                    "label": "Margin",
                    "description": "The gap between the panel and the screen's edges."
                },
                "sidebar.reserveSpace": {
                    "type": "bool",
                    "default": false,
                    "label": "Push windows aside",
                    "description": "While it is open, reserve its width so maximised windows move over instead of being covered. Off: it floats above them and the desktop keeps its shape."
                },
                "sidebar.expanded": {
                    "type": "list",
                    "default": [
                        "media"
                    ],
                    "label": "Open cards",
                    "description": "Which cards start expanded, by id: media, day, weather, machine, notifications. Every card can be folded away and opened again from the sidebar itself; this is what it remembers.",
                    "values": [
                        "media",
                        "day",
                        "weather",
                        "machine",
                        "notifications"
                    ],
                    "labels": [
                        "Playing",
                        "Today",
                        "Weather",
                        "Machine",
                        "Notifications"
                    ]
                },
                "sidebar.cards": {
                    "type": "list",
                    "default": [
                        "media",
                        "day",
                        "weather",
                        "machine",
                        "notifications"
                    ],
                    "label": "Cards",
                    "description": "Which cards the sidebar draws, in order. Leave a card out to hide it entirely.",
                    "values": [
                        "media",
                        "day",
                        "weather",
                        "machine",
                        "notifications"
                    ],
                    "labels": [
                        "Playing",
                        "Today",
                        "Weather",
                        "Machine",
                        "Notifications"
                    ]
                }
            }
        },
        {
            "id": "weather",
            "label": "Weather",
            "icon": "weather-few-clouds",
            "glyph": "partly_cloudy_day",
            "description": "Where the weather comes from. Off until you turn it on, because it is the one part of this shell that talks to the internet: forecasts come from Open-Meteo (no account, no key), and a place you have not named is looked up once from this machine's IP address.",
            "keys": {
                "weather.enabled": {
                    "type": "bool",
                    "default": false,
                    "label": "Show the weather",
                    "description": "Fetches a forecast for your place. Nothing is sent but the coordinates being asked about; nothing is stored but the answer, in memory."
                },
                "weather.place": {
                    "type": "string",
                    "default": "",
                    "label": "Place",
                    "description": "A town or city to look up -- \"Cluj-Napoca\", \"Lisbon\". Left empty, the place is worked out once from this machine's IP address, which is a guess a network can get wrong; naming it is exact and asks nobody."
                },
                "weather.coordinates": {
                    "type": "string",
                    "default": "",
                    "label": "Coordinates",
                    "description": "\"46.77,23.60\" -- latitude, longitude. Set, this wins over the place and no lookup of any kind is made."
                },
                "weather.units": {
                    "type": "enum",
                    "values": [
                        "metric",
                        "imperial"
                    ],
                    "default": "metric",
                    "label": "Units",
                    "description": "Celsius and km/h, or Fahrenheit and mph.",
                    "labels": [
                        "Metric (°C, km/h)",
                        "Imperial (°F, mph)"
                    ]
                },
                "weather.refresh": {
                    "type": "int",
                    "min": 10,
                    "max": 360,
                    "step": 5,
                    "default": 30,
                    "label": "Refresh every",
                    "description": "Minutes between forecasts. The forecast itself changes hourly at best."
                }
            }
        },
        {
            "id": "windows",
            "label": "Windows",
            "icon": "preferences-system-windows",
            "glyph": "select_window",
            "page": "windows",
            "description": "What KWin does with windows: how focus is given, when one is raised, where a new one lands, and whether a maximised window keeps its border. These are KWin's own settings, written through the ledger by `rmpr windows behaviour`, so every change can be undone. Window gaps, rounded window corners and tiling layouts are not KWin's to give, and are not offered here."
        },
        {
            "id": "launcher",
            "label": "Launcher",
            "icon": "start-here-kde",
            "glyph": "search",
            "page": "launcher",
            "description": "What opens when you press the start button, and what opens when you search.",
            "keys": {
                "launcher.provider": {
                    "type": "enum",
                    "values": [
                        "auto",
                        "kickoff",
                        "builtin",
                        "krunner",
                        "fuzzel",
                        "rofi",
                        "custom"
                    ],
                    "default": "builtin",
                    "label": "Application menu",
                    "description": "Kickoff is Plasma's own menu, but it opens at whichever panel holds plasmashell's launcher applet rather than at this one."
                },
                "launcher.searchProvider": {
                    "type": "enum",
                    "values": [
                        "auto",
                        "krunner",
                        "builtin",
                        "kickoff",
                        "fuzzel",
                        "rofi",
                        "custom"
                    ],
                    "default": "builtin",
                    "label": "Search",
                    "description": "KRunner is Plasma's own search."
                },
                "launcher.layout": {
                    "type": "enum",
                    "values": [
                        "twopane",
                        "grid",
                        "list"
                    ],
                    "default": "twopane",
                    "label": "Start menu",
                    "description": "How the built-in launcher's start menu is laid out. twopane: categories, pinned apps and recent files, with you, what is playing and the machine beside them. grid: pinned apps and recent files. list: every application A to Z. Only when the built-in launcher is the application menu."
                },
                "launcher.actionPrefix": {
                    "type": "enum",
                    "values": [
                        ">",
                        ":",
                        "/"
                    ],
                    "default": ">",
                    "label": "Action prefix",
                    "description": "Typed first in the built-in search, it offers the shell's actions -- the colour scheme, the wallpaper, the session, a calculator -- instead of applications."
                },
                "launcher.dense": {
                    "type": "bool",
                    "default": false,
                    "label": "Compact results",
                    "description": "Shorter rows in the built-in search, so more fit."
                },
                "launcher.hints": {
                    "type": "bool",
                    "default": true,
                    "label": "Key hints",
                    "description": "The keys the built-in search answers to, under its results."
                },
                "launcher.searchSources": {
                    "type": "list",
                    "default": [
                        "apps",
                        "windows",
                        "files",
                        "settings"
                    ],
                    "label": "Search results",
                    "description": "What the built-in search looks through. apps: everything installed. windows: the open ones, by their titles, so a window can be raised by name. files: what was opened recently. settings: this shell's own pages. Remove a name to stop searching it."
                },
                "launcher.learn": {
                    "type": "bool",
                    "default": true,
                    "label": "Learn what you open",
                    "description": "What has been opened before is offered first, and an empty search suggests it. Kept in the state directory and never sent anywhere; turning this off stops it being read, and Forget clears what is there."
                },
                "launcher.pinned": {
                    "type": "list",
                    "default": [],
                    "label": "Pinned applications",
                    "description": "Desktop entry ids at the top of the built-in start menu, in this order. Empty picks a terminal, files, a browser, an editor and so on from what is installed."
                },
                "launcher.kickoffMode": {
                    "type": "enum",
                    "values": [
                        "menu",
                        "windowed"
                    ],
                    "default": "menu",
                    "label": "Kickoff style",
                    "description": "Windowed opens Kickoff as an ordinary window; slower, and it will not close itself when it loses focus."
                },
                "launcher.command": {
                    "type": "list",
                    "format": "words",
                    "default": [],
                    "label": "A command of your own",
                    "description": "For the `custom` provider: a launcher and its arguments, run as written. `launcher.provider` or `launcher.searchProvider` set to `custom` runs it."
                }
            }
        },
        {
            "id": "notifications",
            "label": "Notifications",
            "icon": "notifications",
            "glyph": "notifications",
            "page": "notifications",
            "description": "Plasma draws every notification -- under this shell's own renderer, through the Plasma services it hosts -- unless this shell is asked to draw them itself. The history remembers what went past either way, so a notification that disappeared can be read again and, with AI assist on, asked about.",
            "keys": {
                "notifications.server": {
                    "type": "enum",
                    "values": [
                        "plasma",
                        "shell"
                    ],
                    "default": "shell",
                    "label": "Drawn by",
                    "description": "plasma: Plasma's own notification server, as always. shell: this shell serves them and draws its own popups beside the panel, with an Ask button on each when AI assist is on. That replaces Plasma's, so it is off unless chosen. It works only under this shell's renderer, and while another program holds the notification service -- Plasma's hosted applet, or another shell's bar -- it waits for the service to be let go of rather than taking it."
                },
                "notifications.popupTimeout": {
                    "type": "int",
                    "min": 2,
                    "max": 30,
                    "default": 6,
                    "label": "Popups stay for (seconds)",
                    "description": "Unless the application asks for a time of its own. Critical ones stay until closed, and the pointer resting on a popup holds it. Only when this shell draws them."
                },
                "notifications.popupPosition": {
                    "type": "enum",
                    "values": [
                        "auto",
                        "top-right",
                        "top-center",
                        "top-left",
                        "bottom-right",
                        "bottom-center",
                        "bottom-left"
                    ],
                    "default": "auto",
                    "label": "Popups appear at",
                    "description": "auto is the right-hand end of the panel's edge, beside the clock. The centres are the middle of the top or bottom edge. Only when this shell draws them."
                },
                "notifications.centreStyle": {
                    "type": "enum",
                    "values": [
                        "grouped",
                        "stream"
                    ],
                    "default": "grouped",
                    "label": "Notification centre",
                    "description": "What the bell opens. grouped: one card per application, the latest on top and the rest stacked behind it. stream: every notification in order, under today, yesterday and earlier."
                },
                "notifications.history": {
                    "type": "bool",
                    "default": true,
                    "label": "Keep a history",
                    "description": "Listens on the session bus for notifications as they are sent. Nothing is taken over and nothing is stored on disk; the history lives in memory and is gone when the shell stops. Off, the listener does not run at all."
                },
                "notifications.historySize": {
                    "type": "int",
                    "min": 5,
                    "max": 500,
                    "step": 5,
                    "default": 50,
                    "label": "Remember",
                    "description": "How many recent notifications to keep."
                }
            }
        },
        {
            "id": "lock",
            "label": "Lock & session",
            "icon": "system-log-out",
            "glyph": "lock",
            "page": "lock",
            "description": "This shell's own lock screen -- off until it has been tried -- and which screen asks before the session ends. Plasma's greeter does the locking either way.",
            "keys": {
                "session.prompt": {
                    "type": "enum",
                    "values": [
                        "plasma",
                        "shell"
                    ],
                    "default": "plasma",
                    "label": "Ending the session asks with",
                    "description": "plasma: Plasma's own logout screen, as always. shell: the shell's -- log out, restart, hibernate where the machine can, shut down -- which ends the session through Plasma's session manager all the same, so applications are still asked to save."
                }
            }
        },
        {
            "id": "desktop",
            "label": "Desktop",
            "icon": "preferences-desktop-wallpaper",
            "glyph": "wallpaper",
            "description": "What this shell draws on the desktop itself: a rounded frame over the screen's corners, and a clock on the wallpaper. Both are off until asked for, and neither takes a click or reserves any space.",
            "keys": {
                "desktop.border": {
                    "group": "Screen border",
                    "type": "bool",
                    "default": false,
                    "label": "Rounded screen border",
                    "description": "Paints over the screen's corners so the desktop looks inset with rounded corners. Cosmetic: it reserves no space and takes no clicks, and the windows themselves are not rounded -- KWin has no effect for that."
                },
                "desktop.borderInset": {
                    "group": "Screen border",
                    "type": "int",
                    "min": 0,
                    "max": 40,
                    "default": 0,
                    "label": "Border width",
                    "description": "How far in from each edge the frame is painted, in pixels."
                },
                "desktop.borderRadius": {
                    "group": "Screen border",
                    "type": "int",
                    "min": 0,
                    "max": 48,
                    "default": 13,
                    "label": "Corner radius",
                    "description": "How round the painted corners are, in pixels."
                },
                "desktop.clock": {
                    "group": "Desktop clock",
                    "type": "bool",
                    "default": true,
                    "label": "Clock on the desktop",
                    "description": "The time and date on the wallpaper, under every window."
                },
                "desktop.clockPosition": {
                    "group": "Desktop clock",
                    "type": "enum",
                    "values": [
                        "top-left",
                        "top-right",
                        "bottom-left",
                        "bottom-right"
                    ],
                    "default": "bottom-right",
                    "label": "Where it sits",
                    "labels": [
                        "Top left",
                        "Top right",
                        "Bottom left",
                        "Bottom right"
                    ]
                },
                "desktop.clockSize": {
                    "group": "Desktop clock",
                    "type": "int",
                    "min": 40,
                    "max": 200,
                    "default": 92,
                    "label": "How big",
                    "description": "The height of the time, in pixels; the date follows it."
                },
                "desktop.clockDate": {
                    "group": "Desktop clock",
                    "type": "bool",
                    "default": true,
                    "label": "Show the date"
                },
                "desktop.clockInk": {
                    "group": "Desktop clock",
                    "type": "enum",
                    "values": [
                        "auto",
                        "light",
                        "dark"
                    ],
                    "default": "auto",
                    "label": "Ink",
                    "description": "auto follows the colour scheme. Nothing here can read the wallpaper, so a dark clock on a dark picture is one setting away rather than guessed.",
                    "labels": [
                        "Follow the colour scheme",
                        "Light",
                        "Dark"
                    ]
                }
            }
        },
        {
            "id": "services",
            "label": "Plasma services",
            "icon": "preferences-system",
            "glyph": "memory",
            "description": "Plasma's notifications, its clipboard history and its device notifier live inside Plasma's system tray, and the panel this shell draws has no Plasma tray. So under this shell's own renderer they are kept running by hosting Plasma's own applets outside any panel, each showing as one icon in the tray. Nothing is reimplemented, and nothing is hosted where a Plasma tray is there to provide them.",
            "keys": {
                "services.hostPlasma": {
                    "type": "bool",
                    "default": true,
                    "label": "Keep Plasma's notifications, clipboard and device notifier running",
                    "description": "Off, under the quickshell renderer nothing receives notifications at all -- they are dropped, not queued -- and the clipboard widget keeps a history of its own instead of Plasma's."
                },
                "clipboard.history": {
                    "type": "enum",
                    "values": [
                        "own",
                        "auto",
                        "plasma"
                    ],
                    "default": "own",
                    "label": "Clipboard history",
                    "description": "Which history Meta+V shows. Ours keeps text in memory and copied images as files, and an image in it can be chosen -- Klipper's DBus hands out text only, so a picture in Plasma's history can be seen and never picked. 'auto' is Klipper's whenever it is running, ours when it is not; 'plasma' is always Klipper's. Both can exist at once: neither writes to the other.",
                    "labels": [
                        "This shell's own",
                        "Klipper's while it runs",
                        "Always Klipper's"
                    ]
                }
            }
        },
        {
            "id": "widgets",
            "label": "Widgets",
            "icon": "preferences-desktop-plasma",
            "glyph": "widgets",
            "page": "widgets",
            "description": "What appears on the panel, and in which zone."
        },
        {
            "id": "tray",
            "label": "Tray icons",
            "icon": "preferences-system-tray",
            "glyph": "expand_circle_down",
            "page": "tray",
            "description": "Which tray icons sit on the panel, which go behind the chevron, and which are left out. Drag a row from one list to another."
        },
        {
            "id": "edges",
            "label": "Screen edges",
            "icon": "preferences-system-windows-effect-screenedge",
            "glyph": "border_outer",
            "page": "edges",
            "description": "What happens when the pointer is pushed into a corner or an edge of the screen, and whether a window dragged there snaps. KWin does all of it; this only configures KWin, and every change can be undone."
        },
        {
            "id": "shortcuts",
            "label": "Shortcuts",
            "icon": "preferences-desktop-keyboard-shortcut",
            "glyph": "keyboard",
            "page": "shortcuts",
            "description": "Every global shortcut this shell can take, and what each is bound to. These live in KDE's own kglobalshortcutsrc rather than in this shell's profile, so they are not part of a preset and do not move with one. Nothing is bound by default.",
            "keys": {}
        },
        {
            "id": "switching",
            "label": "Switching windows",
            "icon": "preferences-system-tabbox",
            "glyph": "switch_access_2",
            "page": "switching",
            "description": "What Alt+Tab looks like, and which program gets Alt+Tab and Meta+Tab. KWin draws the window switcher and the Overview; another shell running beside it may be holding the keys, and nothing here moves one without being asked.",
            "keys": {
                "switching.windows": {
                    "type": "enum",
                    "values": [
                        "plasma",
                        "shell"
                    ],
                    "default": "plasma",
                    "label": "Alt+Tab is drawn by",
                    "description": "plasma is KWin's own switcher, in this shell's colours. It is the default, it is the only one that can show a picture of each window -- KWin renders those for its own switcher and for nothing else -- and being inside the compositor it sees the keyboard directly. shell is this shell's card row, drawn here, which shows each application's icon instead. Choose it knowing the cost: the shell is not the compositor, so the key going down and the key coming up each cross kglobalaccel, the session daemon, the CLI and the IPC to reach it, and the two race. A quick enough Alt+Tab can leave the switcher on screen after the key is let go. Changing this rebinds Alt+Tab to whichever draws it."
                },
                "switching.desktops": {
                    "type": "enum",
                    "values": [
                        "plasma",
                        "shell"
                    ],
                    "default": "plasma",
                    "label": "Meta+Tab is drawn by",
                    "description": "shell is this shell's own desktop overview -- every desktop, what is open on each, and one more at the end -- drawn here in the panel's colours. plasma is KWin's Overview, which shows a real picture of every window but cannot be restyled: it is compiled into KWin rather than shipped as a package. Changing this rebinds Meta+Tab, taking it from whatever holds it; `rmpr switcher revert` gives it back."
                },
                "switching.overviewHold": {
                    "type": "bool",
                    "default": true,
                    "label": "The overview closes when the key is released",
                    "description": "On, Meta+Tab is held: the desktops are shown while the key is down and letting go switches to whatever is selected, the way Alt+Tab works. Off, one press opens it and it stays until you choose something, press Escape or click away -- which is closer to how Windows behaves."
                },
                "switching.overviewTitles": {
                    "type": "bool",
                    "default": true,
                    "label": "Window titles in the overview",
                    "description": "The title strip along the top of each window card. Off leaves the application's name and its state underneath, and a card that is only the application."
                },
                "switching.overviewMinimised": {
                    "type": "bool",
                    "default": true,
                    "label": "List minimised windows",
                    "description": "Minimised windows appear in the overview, dimmed. Off lists only what is actually on screen."
                },
                "switching.overviewStrip": {
                    "type": "bool",
                    "default": true,
                    "label": "Show the desktop strip",
                    "description": "The row along the bottom with every desktop, what is on each, and a tile for one more. Off gives the whole surface to the selected desktop's windows, and the desktops move on the arrows alone."
                },
                "switching.overviewCardWidth": {
                    "type": "int",
                    "default": 560,
                    "min": 260,
                    "max": 720,
                    "label": "Widest a window card gets",
                    "description": "In pixels. Cards share the room between them, three to a row at most, and never grow past this."
                }
            }
        },
        {
            "id": "sound",
            "label": "Sound",
            "icon": "audio-volume-high",
            "glyph": "volume_up",
            "description": "What a volume control here may do. PipeWire will amplify past 100% and distort doing it, so nothing in this shell offers that headroom until it is asked for -- the same choice, under the same name, as Plasma's own applet.",
            "keys": {
                "audio.raiseMaxVolume": {
                    "type": "bool",
                    "default": false,
                    "label": "Raise maximum volume",
                    "description": "Lets every volume slider in this shell go to 150% instead of stopping at 100%. Above 100% the sound is amplified in software, which distorts on most hardware. A level something else has already set above the ceiling is always shown, switch or no switch."
                }
            }
        },
        {
            "id": "osd",
            "label": "On-screen display",
            "icon": "audio-volume-high",
            "glyph": "display_settings",
            "description": "The volume and brightness popup. Plasma draws it by default and works well; ours exists for the placement and animation a Plasma OSD cannot do. Turning ours on without silencing Plasma's shows both -- 'rmpr theme osd ours' silences it.",
            "keys": {
                "osd.enabled": {
                    "type": "bool",
                    "default": false,
                    "label": "Draw our own",
                    "description": "Listens to the same signals Plasma's OSD does. Nothing is taken over, and turning it off leaves Plasma exactly as it was."
                },
                "osd.timeout": {
                    "type": "int",
                    "min": 500,
                    "max": 5000,
                    "step": 100,
                    "default": 1800,
                    "label": "Visible for",
                    "description": "Milliseconds before it fades."
                }
            }
        },
        {
            "id": "presets",
            "label": "Layouts",
            "icon": "preferences-desktop-theme",
            "glyph": "style",
            "page": "presets",
            "description": "Shipped panel layouts. Applying one replaces your current configuration."
        },
        {
            "id": "profiles",
            "label": "Profiles",
            "icon": "preferences-desktop-display",
            "glyph": "folder_managed",
            "page": "profiles",
            "description": "Separate configurations you can switch between, and per-monitor overrides within each."
        },
        {
            "id": "snapshots",
            "label": "Restore points",
            "icon": "document-save",
            "glyph": "history",
            "page": "snapshots",
            "description": "Snapshots of your KDE configuration. Nothing is removed when reverting or uninstalling. They are removed here, or by pruning if you have set how many to keep -- and pruning never takes one you have locked, nor the oldest, which is the state the machine was in before this shell was installed.",
            "keys": {
                "snapshots.keep": {
                    "type": "int",
                    "default": 0,
                    "min": 0,
                    "max": 200,
                    "label": "Restore points to keep",
                    "description": "How many restore points are kept when a new one is taken. 0 keeps every one of them, which is the default: deleting somebody's restore points without being asked is not a thing to start doing quietly. Two are never removed by pruning whatever this says -- any restore point you have locked, and the oldest, which is the state the machine was in before this shell was installed."
                }
            }
        },
        {
            "id": "renderer",
            "label": "Drawn by",
            "icon": "preferences-desktop-plasma",
            "glyph": "dashboard",
            "page": "renderer",
            "description": "What draws the panel. Only one of these can draw at a time, and switching is a real change to your desktop rather than a setting -- so it happens here, with a restore point, rather than as a value you can type.",
            "keys": {
                "panel.renderer": {
                    "type": "string",
                    "default": "quickshell",
                    "label": "Drawn by",
                    "description": "Which of them draws the panel: `quickshell` (this shell), `plasma`, `none`, or `quickshell:<config>` for any other Quickshell configuration on this machine -- `rmpr renderer list` names them. Only one can draw, so two panels at one screen edge is not a state this can reach. Changing it by hand only tells the shell; the shell package, the applet layout and the restore point are the CLI's job -- use `rmpr renderer set`."
                }
            }
        },
        {
            "id": "ai",
            "label": "AI assist",
            "icon": "help-hint",
            "glyph": "smart_toy",
            "page": "ai",
            "description": "When something breaks, hand a redacted diagnostic report to an assistant. Off by default. Nothing leaves this machine without a confirmation that shows exactly what would be sent.",
            "keys": {
                "ai.enabled": {
                    "type": "bool",
                    "default": false,
                    "label": "AI assist",
                    "description": "Turns on the 'ask' actions: in the notification history, as a global shortcut, and as `rmpr ask`. It also starts the notification listener, since a notification has to have been seen to be asked about."
                },
                "ai.provider": {
                    "type": "enum",
                    "values": [
                        "clipboard",
                        "claude-code",
                        "ollama",
                        "custom"
                    ],
                    "default": "clipboard",
                    "label": "Provider",
                    "description": "`clipboard` copies the report for pasting anywhere and sends nothing. `claude-code` opens the `claude` command with the report. `ollama` asks a local model over HTTP. `custom` runs `ai.command`. Providers whose program is not installed are not offered."
                },
                "ai.command": {
                    "type": "list",
                    "default": [],
                    "label": "Custom command",
                    "description": "For the `custom` provider: a command and its arguments. `%report` is replaced with the path of the redacted bundle; without it, the bundle arrives on standard input.",
                    "format": "words"
                },
                "ai.ollamaUrl": {
                    "type": "string",
                    "default": "http://127.0.0.1:11434",
                    "label": "Ollama address",
                    "description": "Where the `ollama` provider sends its request. An address that is not this machine counts as leaving it, and is confirmed like any other."
                },
                "ai.ollamaModel": {
                    "type": "string",
                    "default": "",
                    "label": "Ollama model",
                    "description": "Empty picks the first model Ollama lists."
                }
            }
        },
        {
            "id": "about",
            "label": "About",
            "icon": "help-about",
            "glyph": "info",
            "page": "about",
            "keys": {
                "update.channel": {
                    "type": "enum",
                    "values": [
                        "main",
                        "dev"
                    ],
                    "labels": [
                        "Releases",
                        "Everything as it lands"
                    ],
                    "default": "main",
                    "label": "Update channel",
                    "description": "Which branch `rmpr update` follows: `main` moves on a release, `dev` is where work lands and moves every day."
                },
                "update.remote": {
                    "type": "string",
                    "default": "",
                    "label": "Update from",
                    "description": "A git URL to update from. Empty uses the checkout's own origin, then the project's public address."
                },
                "update.localSource": {
                    "type": "string",
                    "default": "",
                    "label": "Local source",
                    "description": "A checkout on this machine to update from instead of a remote -- for testing a change before it is pushed. Empty for none."
                }
            }
        }
    ]
}
